package org.firstinspires.ftc.teamcode.ROBOT.UTILS.WRAPPERS.CAMERA

import com.qualcomm.hardware.limelightvision.LLResultTypes.ColorResult
import com.qualcomm.hardware.limelightvision.Limelight3A


class commands {
/*
    var camera: Limelight3A? = null
    var targetCorners: List<ColorResult>? = null
    var cameraResults: List<List<Double>>? = null
    //var corners: MutableList<Double>? = null
    var corner1: Double = 0.0
    var corner2: Double = 0.0
    var corner3: Double = 0.0
    var corner4: Double = 0.0

    fun getCorners(): List<Double>? {
        targetCorners = camera!!.latestResult.colorResults
      //  cameraResults = targetCorners.get(0).targetCorners
        corners!!.add(camera!!.latestResult.colorResults[0].targetCorners[0][0])
        corners!!.add(camera!!.latestResult.colorResults[0].targetCorners[0][1])
        corners!!.add(camera!!.latestResult.colorResults[0].targetCorners[1][0])
        corners!!.add(camera!!.latestResult.colorResults[0].targetCorners[1][1])


        return corners
    }

    var is_open : Boolean = camera!!.isRunning

   // var test = camera!!.

   /* while(is_open) {
        // Pipeline states
        // Needs to be changed
        // 0 - starting position, 1 -
        if () {
            limelight.pipelineSwitch(0);
        } else {
            limelight.pipelineSwitch(1);
        }
    }

    */

 */
}